meta {
  name: DELETE
  type: http
  seq: 12
}

delete {
  url: http://{{url}}:{{port}}/{{version}}/users/{{user_id}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

tests {
  test("delete user returns 204", function () {
    expect(res.getStatus()).to.equal(204);
  });
}
