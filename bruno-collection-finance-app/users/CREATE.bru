meta {
  name: CREATE
  type: http
  seq: 2
}

post {
  url: http://{{url}}:{{port}}/{{version}}/users
  body: json
  auth: inherit
}

body:json {
  {
    "name": "string",
    "lastname": "string",
    "username": "string",
    "email": "user@example.com",
    "password": "stringst"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
